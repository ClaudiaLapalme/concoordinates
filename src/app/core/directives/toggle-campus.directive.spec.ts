import { ToggleCampusDirective } from './toggle-campus.directive';

describe('ToggleCampusDirective', () => {
    it('should create an instance', () => {
        const directive = new ToggleCampusDirective();
        expect(directive).toBeTruthy();
    });
});
